# Express Project Progress Checkpoint

## Status: All core modules complete — 100/100 tests passing
- **Test score**: 100/100 passing (0 failures)
- **Last updated**: 2026-06-28

## Completed
1. **Config** (`express/config.py`) — Dataclass config, env var overrides, workspace init
2. **LLM layer** (`express/llm/client.py`, `express/llm/prompts.py`) — Async OpenAI-compatible client, code generation & self-heal system prompts
3. **Renderer** (`express/renderer/engine.py`, `express/renderer/framebuffer.py`) — Usagi process manager, headless execution, 160x90 framebuffer capture
4. **Self-heal** (`express/self_heal/evaluator.py`) — Console log analyzer, Lua error pattern detection
5. **MCP tool** (`express/tools/render_expression.py`) — Full pipeline orchestration (LLM → Engine → Framebuffer → Self-heal), `_extract_lua_code` with prose-vs-code heuristic
6. **MCP server** (`express/mcp_server.py`) — MCP stdio transport, tool registration, JSON-RPC handlers
7. **Entry point** (`express/__init__.py`) — Minimal-context imports for all subsystems
8. **Tests** — 8 test files, 100 tests, all passing

## Test Files
- `tests/test_config.py` — Config initialization, env var overrides, workspace/skeleton generation
- `tests/test_engine.py` — Engine manager, headless execution, timeout handling, binary not found
- `tests/test_evaluator.py` — Console analysis, error pattern matching, duplicate removal
- `tests/test_framebuffer.py` — Framebuffer capture (fbgrab/fb0/screenshot), PNG processing, base64
- `tests/test_llm_client.py` — LLM client init, response parsing, reasoning extraction, context manager
- `tests/test_prompts.py` — System prompt validation, palette, resolution, API coverage
- `tests/test_render_expression.py` — Integration tests (success, crash+heal, LLM failure), code extraction
- `tests/test_mcp_server.py` — Tool registration, list_tools, tool call handling, error cases

## Architecture
```
express/
├── __init__.py          # Navigational entry point
├── config.py            # Configuration dataclass + workspace init
├── mcp_server.py        # MCP stdio server, tool registration
├── llm/
│   ├── __init__.py
│   ├── client.py        # AsyncOpenAIClient, response parsing
│   └── prompts.py       # CODE_GENERATION_SYSTEM, SELF_HEAL_SYSTEM
├── renderer/
│   ├── __init__.py
│   ├── engine.py        # EngineManager, run_headless, timeout handling
│   └── framebuffer.py   # FramebufferCapture, PNG processing, 160x90
├── self_heal/
│   ├── __init__.py
│   └── evaluator.py     # ExpressionEvaluator, error pattern matching
└── tools/
    └── render_expression.py  # render_expression(), _extract_lua_code(), _looks_like_code()
```

## Run tests
```bash
cd /home/monaghan/.build/express && python -m pytest tests/ -v --tb=short
```

## Key Design Decisions
- `_extract_lua_code` uses prose-vs-code heuristic to detect LLM refusals (returns None for prose)
- Engine timeout handling uses list-based mock side_effect for sequential call simulation
- Config defaults bypass class-body evaluation via keyword arguments in tests
- Self-heal evaluator uses case-sensitive pattern matching for Lua runtime errors
- MCP server uses `@app.list_tools()` and `@app.call_tool()` decorators per MCP SDK v1.26.0
- All external dependencies (LLM API, Usagi binary, DRM framebuffer) mocked in tests